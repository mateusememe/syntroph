# Stdio MCP storage provider

The first MCP provider launches an explicitly configured stdio command and speaks MCP without handling authentication. Before any mirror effect it discovers and validates equivalent capabilities for issue read, create/update, comments, labels, and issue search; a read-only or incomplete server records Storage Prerequisite Missing. HTTP MCP is deferred until the stdio contract is proven.
