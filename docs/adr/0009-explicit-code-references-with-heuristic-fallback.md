# Explicit code references with heuristic fallback

Session Artifacts carry explicit code references in JSON and an equivalent Markdown section. Runtimes supply the authoritative references; the Core may attempt heuristic extraction only as best effort, preserving runtime independence without presenting guessed references as authoritative.
