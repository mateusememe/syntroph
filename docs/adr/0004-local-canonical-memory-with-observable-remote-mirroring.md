# Local canonical memory with observable remote mirroring

The initial canonical copy of every Session Diary is stored under `.syntroph/memory/`. GitHub Wiki and GitHub Issues are real selectable mirror adapters, but an unavailable remote creates Storage Sync Pending with enough state for the user or a local integration to inspect and retry it; a missing token or network never loses a diary.
